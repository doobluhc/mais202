# mais202 final project ----  NBA outcomes prediction

dataset used for logistic regression and SVM ---- nba.games.stats.csv

logistic regression and SVM model ---  FinalProject.py

decision tree model --  decisiontree.py

NOTE: the dataset used for the decision tree model is directly scraped from the internet in decisiontree.py
